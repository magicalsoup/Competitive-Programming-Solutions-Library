The positive edge length cases can be handled using Dijkstra's algorithm, 
but the negative case requires Bellman–Ford. You might as well just use the latter for all the cases, since it's easier to code.
